{
  "compilerOptions": {
    "target": "ES2022", // Compile TypeScript down to modern JavaScript (ES2022 features).
    "module": "NodeNext", // Use the Node.js module system with support for both CommonJS and ES
    "rootDir": ".", // Allow source files at project root and inside src
    "moduleResolution": "nodenext", // Control how modules are found — matches Node.js’s latest module resolution rules.
    "isolatedModules": true,
    "outDir": "./dist", // Put all compiled JavaScript files into the dist folder.
    "esModuleInterop": true, // Makes it easier to import CommonJS modules with import syntax.
    "forceConsistentCasingInFileNames": true, // Ensures file imports are case-sensitive.
    /* Type Checking */
    "strict": true, // Enables strict type checking for safer, less error-prone code.
    "skipLibCheck": true // Skip type-checking for files inside node_modules to speed up compilation.
  },
  "include": [
    "src/**/*",  //Compile everything inside src (any extension)
    "index.ts" // Also include the root-level entry file
, "__tests__"  ],
  "exclude": [ // Don’t compile external libraries or test files (.spec.ts).
    "node_modules",
    "**/*.spec.ts"
  ]
}